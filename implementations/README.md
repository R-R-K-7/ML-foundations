## Introduction
* This project containes implementations of common machine-learning algorithms - `KMeansClustering`, `DecisionTrees`, `PrincipalComponentAnalysis(PCA)`, `NeuralNetworks` entirely using Numpy.
* Each class is tested using synthetically generated datasets. 

## Dependencies
* `numpy`
* `matplotlib`
